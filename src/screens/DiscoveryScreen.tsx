import React from 'react';\nimport { View, Text, Button, FlatList, StyleSheet } from 'react-native';\n\nconst DiscoveryScreen = () => {\n  const users = [\n    { id: '1', name: 'John Doe', description: 'Software Engineer' },\n    { id: '2', name: 'Jane Smith', description: 'Product Manager' },\n    // Add more user data as needed\n  ];\n\n  const renderItem = ({ item }) => (\n    <View style={styles.card}>\n      <Text style={styles.name}>{item.name}</Text>\n      <Text style={styles.description}>{item.description}</Text>\n      <Button title='Call' onPress={() => handleCall(item.id)} />\n    </View>\n  );\n\n  const handleCall = (userId) => {\n    // Logic for initiating a call\n    console.log(`Calling user with id: ${userId}`);\n  };\n\n  return (\n    <View style={styles.container}>\n      <Text style={styles.title}>User Discovery</Text>\n      <FlatList\n        data={users}\n        renderItem={renderItem}\n        keyExtractor={item => item.id}\n      />\n    </View>\n  );\n};\n\nconst styles = StyleSheet.create({\n  container: {\n    flex: 1,\n    padding: 20,\n  },\n  title: {\n    fontSize: 24,\n    fontWeight: 'bold',\n    marginBottom: 20,\n  },\n  card: {\n    backgroundColor: '#fff',\n    padding: 15,\n    marginVertical: 10,\n    borderRadius: 8,\n    shadowColor: '#000',\n    shadowOffset: { width: 0, height: 1 },\n    shadowOpacity: 0.2,\n    shadowRadius: 1.41,\n    elevation: 2,\n  },\n  name: {\n    fontSize: 18,\n    fontWeight: 'bold',\n  },\n  description: {\n    fontSize: 14,\n    color: '#666',\n  },\n});\n\nexport default DiscoveryScreen;